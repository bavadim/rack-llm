# Experiments

This directory contains reproducible paper experiments for the `rack-llm`
controlled soft generation method.

Each experiment subdirectory is self-contained:

- `code/` contains scripts and tests needed to reproduce the artifacts.
- `data/`, `results/`, or `figures/` contain generated artifacts.
- `EXPERIMENT.md` describes the experiment in IMRD format.
